{{- /*
SPDX-FileCopyrightText: 2026 The SonicRed contributors.
SPDX-License-Identifier: MPL-2.0
*/ -}}

apiVersion: v1
kind: Pod
metadata:
    name: "{{ include "SonicRed.fullname" . }}-test-connection"
    labels:
        {{- include "SonicRed.labels" . | nindent 8 }}
    annotations:
        "helm.sh/hook": test
spec:
    containers:
      - name: wget
        image: busybox
        command: ['wget']
        args: ['{{ include "SonicRed.fullname" . }}:{{ .Values.service.port }}']
    restartPolicy: Never
